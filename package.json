{
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass:watch": "sass --source-map -w    sass/style.scss css/style.css",
    "sass:debug": "sass --source-map       sass/style.scss css/style.css",
    "sass:release": "sass --style=compressed sass/style.scss css/style.css"
  },
  "author": "interactive",
  "license": "ISC",
  "devDependencies": {
    "sass": "^1.23.0",
    "sass-loader": "^13.3.1"
  }
}

